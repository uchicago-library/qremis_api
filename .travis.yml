language: python
python:
    - "3.5"
before_script:
    - pip install coverage && pip install python-coveralls
install: "pip install . && pip install git+git://github.com/bnbalsamo/pyqremis.git"
services:
    - redis
script: coverage run --source qremis_api tests.py
after_success:
    - coveralls
